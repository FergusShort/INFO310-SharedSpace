<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up</title>
  <link rel="stylesheet" href="/logInGroups.css">
</head>
<body>
  <div class="container">
    <h2>Sign Up</h2>

    <% if (error) { %>
      <p style="color: red;"><%= error %></p>
    <% } %>

    <form action="/signup/submit" method="POST" enctype="multipart/form-data">
      <label for="Uname">Username</label>
      <input type="text" id="Uname" name="Uname" required value="<%= formData?.Uname || '' %>">

      <label for="email">Email</label>
      <input type="email" id="email" name="email" required value="<%= formData?.email || '' %>">

      <label for="pwd">Password</label>
      <input type="password" id="pwd" name="pwd" required>

      <button type="submit">Sign Up</button>
      <p>Already have an account? <a href="login">Login</a></p>
    </form>
  </div>
</body>
</html>
